{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/syscall",
      "packages/utils",
      "packages/app"
    ],
    "nohoist": []
  },
  "version": "0.0.0",
  "scripts": {
    "dev": "yarn workspaces run dev",
    "build": "yarn workspaces run build"
  }
}
